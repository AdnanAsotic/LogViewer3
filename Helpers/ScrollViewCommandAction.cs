using Prism.Interactivity;

namespace LogViewer3.Helpers
{
    public class ScrollViewCommandAction : InvokeCommandAction
    {
        protected override void Invoke(object parameter)
        {
            base.Invoke(parameter);
        }
    }
}
